#include "convert.h"

void doCommand(string command) {
  if (command.find("disassemble")) {
    
  }
}

int main() {
  while (true) {
    string command;
    
    cin >> command;
    
    doCommand(command);
  }
}
